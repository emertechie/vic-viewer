# Vic-Viewer Environment Configuration
#
# Copy this file to .env and customize the values to override defaults.
# The .env file is gitignored and won't be committed.

# =============================================================================
# Port Configuration (change these to avoid conflicts with other instances)
# =============================================================================

# UI development server port (default: 5173)
# UI_PORT=5173

# API server port (default: 4319)
# API_PORT=4319

# =============================================================================
# Other Configuration
# =============================================================================

# You can also override any other value `src/server/config.ts` here, for example:
# Log profile configuration file path
# LOGS_PROFILE_PATH=./config/log-profiles/dotnet-opentelemetry.yml
